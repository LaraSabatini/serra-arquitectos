import styled from "styled-components"

const SearchContainer = styled.div`
  padding: 15px 0;
`

const ErrorMessage = styled.p`
  color: red;
  font-size: 14px;
  margin: 0;
`

export { SearchContainer, ErrorMessage }
